/* MODELO: copie este arquivo para assets/content/<curso>/posicoes-trilha.js.
   Troque "id-do-curso" pelo mesmo ID usado em curso.js.
   Cada item do array corresponde a um módulo: índice 0 = módulo 1.

   (function () {
     'use strict';
     window.CL = window.CL || {};
     window.CL.trilha = window.CL.trilha || {};
     window.CL.trilha.POSICOES = window.CL.trilha.POSICOES || {};

     var pontos = [
       [0, 0] // módulo 1: ajuste horizontal, ajuste vertical
     ];

     window.CL.trilha.POSICOES['id-do-curso'] = pontos.map(function (ponto) {
       return { x: ponto[0], y: ponto[1] };
     });
   })();
*/
